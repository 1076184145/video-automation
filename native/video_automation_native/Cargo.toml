[package]
name = "video_automation_native"
version = "0.1.0"
edition = "2021"

[lib]
name = "video_automation_native"
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
pyo3 = { version = "0.22.6", features = ["extension-module"] }
